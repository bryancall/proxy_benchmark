# ATS remap.config - URL mapping for benchmarking
# Replace {{ORIGIN_HOST}} and {{ORIGIN_PORT}} with actual values

# Map all requests to origin server
map http://localhost:8080/ http://{{ORIGIN_HOST}}:{{ORIGIN_PORT}}/
map https://localhost:8443/ http://{{ORIGIN_HOST}}:{{ORIGIN_PORT}}/

